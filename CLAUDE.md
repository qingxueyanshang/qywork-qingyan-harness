# CLAUDE.md — qywork 开发规则

按你现在在干什么找：**写方案 → A · 写代码 → B · 判断做完没 → C · 写文档和记忆 → D · 碰安全边界 → E**。
F 是明确不采纳的，写出来是为了不被重新抄进来。

规则里不写会过期的数字（测试数、文件数、行数）——要现状就去跑一次。

---

# A　写方案

## A1　每条都要核过码

方案里的每条待办必须标注四项：**依据**（具体到 `文件:行号`，或一次可复现的实测输出）·
**为什么必须做**（不做会怎样、谁会被骗）· **依赖谁** · **真实成本**。

- **无路径 = 猜测 = 不准进方案。** 「感觉这里会有问题」不是依据。
- **子代理、外部模型、对抗评审的意见默认不进方案权威。** 自己打开文件核对后才能写成
  优化项；核不实的直接删，不是降级成「潜在风险」。票数多、推演完备都不构成实现权。
- **优化项 = 怎么修到需求**，不许写成「所以这个做不了」。业务否决单独提出来让用户拍板，
  不能伪装成技术结论混在方案里。

## A2　方案准入门禁

这里的「分支」指业务逻辑分叉、第二条执行路线、第二套真源、隐藏旁路，不是 git 分支。

**写代码前先答完五问**，答不全就继续取证，不得先实现再解释：

1. **当前唯一权威**：现在由哪个模块 / 状态 / 存储对该行为作最终裁决？
2. **最早错误点**：错误状态第一次在哪里被产生，而不是最后在哪里被看见？
3. **原地改点**：如何直接修正现有权威；不能原地修的话，为什么？
4. **删除清单**：会同步删掉哪些旧接口、旧状态、旧分支、旧测试、旧说明？
5. **路径增减**：修复前后真源数、状态账数、执行入口数、持久化路径数是否增加？

**第 5 问只要不是明确的「否」，先停下来说明，不要直接实施。**

命中任一项，方案**默认否决**——确属根因级大改时，先讲清现有权威为什么承载不了、
会删什么、规模多大，由用户拍板：

- 新增**与现有权威并列**的状态机、生命周期、执行入口或后台补偿流；
  判据是「多出一份能独立作数的账」，不是「多出一处代码」——
  同一个权威被第二处调用（同一份状态、同一条落库路径、同一套失败语义）不算命中，
  它是调用点不是权威。把这两者混为一谈的代价实测付过：
  「容量拒绝后压缩重发」被这一条挡了下来，而它调的正是同一个 `CompactionPort`，
  结果是失败路径没有可达终态（C1 第 2 款），会话撞窗后无解。
- 旧接口 / 旧格式 / 旧路径不删，再叠新的或加兼容兜底；
- 错误状态已经产生后，靠下游拦截、重试、改写、定时清理或 UI 遮盖修正；
- 为尚未复现的假设扩大修复范围，或把架构理想混进一个已有明确因果链的具体 bug；
- 新方案不能让旧错误逻辑消失，只能靠「旧逻辑 + 新守卫」共同维持正确。

**根治判定**（同时满足才算）：错误在源头结构性地不再产生（不是下游识别后阻止）·
旧路径随同一次改动删除 · 权威不并存 · 分支数与状态组合不增加 ·
**验收直接复现原始失败形状**（只测新增保护分支命中不算）。

**这一节是规则不是宪法。** 挡住一件技术上正确的事时，先问是规则写粗了还是方案错了；
写粗了就改规则并留下改的理由，不要拿它当挡箭牌，也不要为了绕开它把方案做拧。

## A3　计划文档的撰写模式

每份排查 / 修复 / 重构计划都必须自带进度，让上下文丢失后接手者**读文件顶部即可恢复
进度感知**——不依赖聊天记录、不靠记忆。三个构件缺一不可。

**① 顶部执行进度看板**（标题后第一个区块）：

```markdown
## ▶ 执行进度看板

> 状态图例：⬜ 未开始　🔄 进行中　✅ 已完成待验收（自测通过）　☑️ 已验收（用户亲测通过）　⬛ 已取消/毙掉

- **最后更新**：YYYY-MM-DD · <提交 sha>
- **当前批次 / 下一步**：一句话——接手者第一眼要看的「现在该干什么」
- **阻塞项**：无 / 具体阻塞（等谁、等什么）

| 批次 | 状态 | 进度 |
|---|---|---|
| 批 0　xxx | ✅ 已完成待验收 | 6/6 |
```

**② 五态，只进不退**（回归除外）：⬜ 🔄 ✅ ☑️ ⬛（毙掉要记一句理由）。
**✅ 只有自测通过能标，☑️ 只有用户亲测通过能标——自己不能替用户验收。**
回归重开的标回 🔄，记原因，看板计数同步回退。

**③ 项级状态前缀 + 进度尾字段**：每项标题前加状态标记；完成时在
「位置 / 问题 / 解法 / 验证 / 工作量」后追加
`进度：<状态> · <执行者> · <提交 sha> · <验证结果> · <日期>`。

**更新纪律**：每完成一项，同步回写三处——项级标记、进度尾字段、顶部看板。
**严禁改完代码不回写进度。**

**没验到的要写出来**：验了两端没验中间，就写「不算作已验证」，不要用「应该没问题」
糊过去（样例：`ROADMAP.md` §37.4 / §38.6）。

## A4　大改动走四节流程

**方案节**（按 A1 写、按 A2 自检）→ **复审节**（逐条攻击自己，产出应该是「推翻了几条」，
全票通过的复审等于没做）→ **修订版计划**（套 A3 的看板）→ **执行记录节**。

新计划写进 `docs/plans/YYYY-MM-DD-<主题>.md`，索引挂 `docs/plans/INDEX.md`。
`ROADMAP.md` §1–§39 是历史记录，原地保留不迁移。

## A5　排查先证据，简单优先

- **先拿硬对照**：日志 / 上游返回的原字段 / 落盘的字节——谁和谁一样、谁被本地改过。
  没有对照前不讲长篇机制。
- **用户给的做法先当默认方案验**，只有证伪后再谈复杂原因。
- **历史文档和旧记忆是线索不是结论**，与当前代码冲突时以代码为准。
- 判断编码问题**只能看字节**，终端输出本身会二次污染证据。

---

# B　写代码

## B1　SOLID

不是教科书背诵，是这个仓库里已经成立的五条边界。**每条都有正例，破坏它的地方也点了名。**

**S 单一职责——一个文件一个职责，涨到看不完就拆。**
正例：`packages/server/src/` 里 `files` / `git` / `runs` / `pairing` / `bus` 各管一件事。
反例：同目录的 `server.ts` 同时装着握手、指令分发、run 生命周期、team 编排、压缩、
整个 HTTP API、配置脱敏、静态托管——**拆分模式就在它旁边，它只是没跟着拆**。
判据不是行数是理由数：说不出「这个文件只做 X」就该拆。

**O 对扩展开放、对修改关闭——加东西不该改老代码。**
正例：`ToolRegistry.register()`（`packages/agent/src/registry.ts`）+
`registerBuiltinTools()` 遍历数组注册；加一个内置工具不用碰 agent 循环，
插件工具在其后追加、不得覆盖同名。
新增能力时如果必须改中心调度的 `switch`，先想清楚为什么不能走注册。

**L 里氏替换——拿到接口就只用接口，不要回头判具体类型。**
`buildAdapter()`（`packages/ai/src/factory.ts`）按 `profile.kind` 分派**一次**，
之后调用方只见 `LlmAdapter`。**下游不许再 `instanceof` 或判 kind。**
推论（这条踩过）：**任何「从名字推断行为」的便利都要先问反例是什么。**
`kind` 决定协议，不按模型名猜厂商——经中转站以 OpenAI 协议调 Claude 是常见配置。

**I 接口隔离——只交出对方要用的那几个方法。**
正例：`SinkPort`（`registry.ts`）只有 `land` / `read` / `stat` 三个方法，
而不是把整个内容库交给工具；且 `null` 是合法值，**工具必须能在没有 sink 时降级工作**。
反例苗头：同文件的 `ToolContext` 已经十几个字段，再往里塞之前先问该不该独立成 port。

**D 依赖倒置——依赖朝抽象走，方向朝上不回头。**
包依赖图是无环 DAG：`core` 不依赖任何人，方向一路朝上到 `cli`。
`SinkPort` 的接口定义在 `agent` 包而实现由 `runtime` 注入——
**接口放在上层、实现放在下层，正是因为反向引用会成环**（原注释已写明）。
加依赖前先看方向；需要回边时说明为什么，不要偷偷加。

## B2　简单优先，改动外科手术式

- **最小的、能解决问题的代码。** 不做没要求的功能，不为单次使用造抽象，
  不加没人要的「灵活性」，不为不可能发生的场景写错误处理。
  写了很长一段时先问：资深工程师会不会说这过度复杂了？
- **只碰必须碰的。** 不顺手「改进」相邻代码、注释或格式；不重构没坏的东西；
  匹配现有风格即使你不这么写。发现无关的死代码，**说出来，不要顺手删**。
- **清理自己制造的孤儿**：因你的改动而失效的 import / 变量 / 函数要删掉；
  存量死代码除非被要求否则不动。
- 判据：每一行改动都能直接追溯到用户的请求。

## B3　禁止兼容代码

当前是开发期，没有生产数据也没有外部用户：**不留旧格式/旧接口的兼容层、双写路径、
兜底分支、hidden alias、第二套账、feature flag 旁路。** 改格式改接口就原地改干净，
旧路径随同一次改动删除。

不以「怕还有谁在用」为由保留旧实现——这里没有还在用的人；兼容代码本身就是
兼容性问题的来源（双路径漂移、两本账）。发现存量兼容层或预判某处会长成兼容负担，
**明确告诉用户**，由用户定夺清理时机；不擅自新增，也不擅自扩大保留范围。

例外：**已落盘的键名与迁移标记是历史事实，一律不改**——改了就读不回旧数据。

## B4　禁止补丁式修复

- 不为堵症状加特判 `if/else`、层层 `try` 兜底、针对特定输入的硬编码分支——
  **分支每多一条，根因就埋深一层。**
- 动手前先回答「为什么会走到这里」。修的是产生症状的那一环，不是症状本身。
- 修完的路径必须比修前更简单更正确，不接受「能跑就行」的堆叠。
- 逐一核实修复点的调用方，不允许「这边修好、那边悄悄坏」。

## B5　不造空壳

**没有真实数据源的功能，不做界面。**

「插件市场」在没有 registry 的前提下必然是空页面——那不是先把框架搭好，
是把上一轮删掉的空壳换个名字再造一遍。所以只做**已安装**：它有数据源。

同类一并按 C1 第 1 款处理（**删掉，不是留着等以后接**）：点了没反应的按钮、
和键盘无关的快捷键提示、渲染层认得而后端从不发送的状态值、
依赖本机跑不起来的工具的脚本入口。

能力在某端不存在时（如手机端没有 PTY），握手里声明 `false`、界面不显示入口，
而不是显示一个点了报错的按钮。

## B6　代码怎么摆

- **包内文件数 ≤ 8 就平铺，超过才分子目录。** 现状不一致（`core` 分了
  `domain/` + `protocol/` 两级，其余包平铺，`ai` 只有 `providers/` 一层），
  所以**按这条判，不要照抄最近的那个邻居**。
- **新增包必须在 `scripts/dependency-graph.test.ts` 里登记层号**，
  否则那个测试直接红——漏登记不能表现为悄悄放行。依赖只能朝更底层走，
  同层之间也不许互依。
- **测试文件不强求与源文件 1:1**。一个 test 覆盖多个源文件是允许的
  （`tools.test.ts` 覆盖 `files.ts`，`mcp.test.ts` 覆盖 `client.ts`），
  但**必须在 test 顶部注释里列出覆盖范围**。不写的代价是真实的：
  这份仓库的结构审查里，「这个文件没有测试」的判断就被这种隐式对应关系骗过一次。
- **导出用具名，不用 `export *`**。后者会把内部符号一并泄漏出包边界，
  而且看不出这个包到底对外承诺了什么。

## B7　UI 文案少而精

**界面上不出现解释性文本。** 界面只放三样：可操作的东西、它的名字、它的当前值。
凡是「说明一件已经显示出来的事为什么是这样」的句子，一律不写。
空状态默认不写引导文案，空着即可；错误提示一句话能懂就停，不括注、不枚举操作路径。
设计时先问：**删掉这句用户还能不能用？能 → 删。**

例外是**能力边界声明**必须留全，且不许折叠或降对比度——
「仅应用运行时触发」「重启后生效」这类不是解释，是边界。

**叫「边界」有门槛：界面上没有第二处说得出这件事。** 有第二处它就是解释，删。
踩过的样例：逐请求表下面那句「结果不明：发出去之后连接就断了，对端收没收到、
计没计费无从确定」——那一行的「结果」列写着「结果不明」，token 与金额列写着 N/A，
这句话一个字都没多说。

### 用词只有两个来源：行业通用术语，或产品里已经出现过的词

**自造词一律不要。** 判据是「用户在别处见过这个词吗」——没有就换通用说法；
换不掉，说明这件事本来就不该出现在界面上。

**协议内部字段不进界面。** 判据：这一格填什么要靠读端点文档或读错误原文才能定，
那它属于配置文件，不属于设置页。控件的选项如果是内部枚举名，这个控件就不该存在。

踩过的样例：模型库里的「思考怎么发」「回传推理原文」「缓存路由 / 亲和键」——
三个下拉，选项分别是 `adaptive_only` / `reasoning_text` / `prompt_cache_key`，
用户既判断不了也不该判断。全部删掉，这三项仍可在 `config.json` 里改，落盘前有词表校验。

**边界也只给一句，且只说边界本身**：标题里已经说过的不重复，被影响的东西不逐项
枚举。「这条会话连同它的全部消息、执行记录一起删掉，删了拿不回来」→「删了拿不回来」。
把一句解释改称「边界」不能让它留下来——判据仍是「删掉这句用户还能不能用」。

### 同一条信息只印一处，且印在出问题的那一层上

一件事界面上已经有地方说了，别处就不再印，哪怕别处更详细。判据是**答的是不是同一个
问题**，不是措辞一不一样。

**报错挂在出错的那一层。** 一次请求失败就记在那一次请求那一行，整轮停了才记在轮次上。
把请求级的失败提上来挂在轮次上，用户拿到的是一块说不出「哪一次」的红框。

踩过的样例：运行面板展开区顶上那块红框（`provider_unavailable` + provider 原文）——
会话流末尾的读数条已经印过同一段正文，而失败的是表里第 2 次与第 2.2 次请求，
它们的「结果」列本来就写着这个码。红框删掉，行上那枚「模型服务出错」标记留着。

## B8　不主动造提示卡

**这条只管一类东西：没人要的悬停提示卡。** 用户点了才出现的弹窗、菜单、
确认框不在此列——那些是他要的交互，该做的做全。

不做的是：鼠标划过就自己冒出来、承载的信息在别处本来就看得到、
用户从没提过的那种卡片。别处见过不等于这里要有。

踩过的样例：项目行的悬停卡片（名称 / N 个会话 / 路径）——路径悬停 `title` 就有，
会话数在列表里数得出来，它唯一的效果是鼠标划过时糊住下面那一行。

**每个浮层的样式必须自带一条完整规则，不许和别的浮层共用选择器。**
共用过一次，代价是删掉其中一个浮层时把另一个的定位、边框、底色、投影一起带走
——菜单退化成一堆裸文字，而 CSS 不会为此报任何错。

## B9　浮层尺寸不随内容变

**弹窗 / 对话框 / 菜单的尺寸一旦画出来就不许再变。** 用户在它上面瞄准按钮，
尺寸一变按钮就跑了——上一秒对着「创建项目」，下一秒那个位置是「取消」。

具体到写法：**会随状态换形态的那一块要固定高度**，内容长了在框内滚动，
不把外面撑开。`min-height` 与 `max-height` 取同一个值，不要让内容去撑。

踩过的样例：新建 work 弹窗里「源文件夹」那一格，空态是一块虚线框、
选中态是一行路径，两者高度不同，于是选一次文件夹整个弹窗就缩一截。

这条**优先于**「少即是多」：宁可空态留白多一点，也不许尺寸跳。

## B10　注释只写用途和坑

注释的读者是**下一个要改这里的人**。他要的只有两样：**这段是干什么的**，
**改它会怎么坏**。除此之外的一律不写。

### 文体：标准技术文案

**陈述句、术语准确、一句一个事实。** 不用口语，不做叙事，不打比方，不带语气。

不写的具体形状：

- **场景铺陈与后果剧本**：「表现就是…」「你会看到…」「用户以为…」「一路抖过去」；
- **反问与自问自答**：「那不正是…」「而这恰恰…」；
- **强调堆叠**：整段加粗、连用破折号、括号里再补一句旁白；
- **拟人与比喻**：「它赖着不走」「那个数会撒谎」；
- **自造术语**：把一件有通用说法的事另起一个名字。读的人无从查证，
  也不会在别处见到第二次。协议之间的参数差异写「参数格式」不写「方言」，
  POSIX 与 PowerShell 的差别写「语法」，`prompt_cache_key` 就写字段名本身。

同一件事的标准写法是**结论 + 一句成因**，成因只写机制，不写现象：

> ✗ 点一条会话切过去，`⋯` 鼠标移开还赖着，直到别处抢走焦点才忽然消失。
> ✓ 用 `:has(:focus-visible)`：`:focus-within` 对鼠标点击留下的焦点同样成立。

### 只写这三类

- **用途**：这个文件 / 函数 / 常量负责什么，一两句说完。
- **坑**：为什么不能换成看起来更自然的那种写法，换了会怎么坏。
  写成祈使句——「不要改成 X，因为 Y」，不是「我当时试过 X」。
- **边界**：调用方必须知道的约定（`null` 是合法值、必须先 A 再 B、这个数是估算不是真值）。

一个常数配一句「这个数从哪来的」就够了；推导过程超过三行，说明该写进 `docs/plans/`。

### 禁止写的四类

1. **来源与出处。** 不出现任何外部项目名、别人仓库的路径与 `文件:行号`，
   也不出现「移植自」「照搬」「抄自」「参照物」「参照实现」「原版」「上游那边」。
   这类话对读代码的人零价值——他打不开那个文件，也无从核实；而**看起来像出处的
   东西最容易被当成依据**，下一个人会照着一个他从没读过的实现改这里。
   要写来源，写进 `docs/plans/*`，那是唯一允许记来源与经过的地方。
2. **变更史与自述。** 「原来是 X」「上一版」「这条订正过一次」「我一开始以为」
   「教训写在这里」——这些是 git log 和计划文档的内容。要留的是**结论**：
   把「原来写成 A，后来发现不行，所以改成 B」压成「必须是 B，写成 A 会 …」。
3. **复述代码。** 读一遍函数体就知道的事不写。
4. **过期的断言。** 注释和代码冲突时，错的是注释——当场改，不要在旁边加一句「但是」。
   改行为时**同一次改动**里把讲这件事的注释一起改掉，这不是可选的收尾工作。

### 篇幅

文件头最多列几条不变量；函数上方一段说完，**一条注释只说一件事**。
**注释里不写 `##` 分节的长文**——需要分节就说明它是一篇文档，该去 `docs/plans/`。
判据同 B7：删掉这段，下一个人还改不改得对？改得对 → 删。

### 词表由门禁执行，不靠评审

上面「不写的具体形状」里能按词判定的那几类，落成了 `scripts/comment-style.ts` 的词表，
由 `scripts/comment-style.test.ts` 在 `bun test` 里全量扫描注释，**命中即失败**，
失败信息给「文件:行 + 命中词 + 改写方向」。

覆盖六类：口语与语气词（`其实` `到底` `于是` `顺手` `莫名其妙` …）· 泛指名词
（`东西` `一堆` —— 换成状态 / 对象 / 条目 / 内容）· 第一人称与变更史（`我们` `我`
`上一版` `曾经` `当时` …）· 拟人与比喻（`悄悄` `吃掉` `活着` `炸掉` `干活` `挂掉` …）·
场景铺陈（`以为` `表现就是` …）· 外部出处（`抄自` `照搬` `参照实现` …）。
加词只加到那张表里。

四条纪律：

- **不设豁免名单。** 豁免名单是第二本账；命中就改写，改不动说明这句话本身不该在注释里。
- **扫描范围按包根划，不按 `src` 划**：`vite.config.ts`、`build.rs`、`start.ps1`、
  `Cargo.toml` 里的注释同样是注释，按 `src` 划会把它们漏在外面。`.ps1` / `.toml`
  走 `#` 行注释。
- **只扫注释，不扫字符串。** 界面文案与提示词归 B7，判据不同——但同一批词在那边
  同样不该出现，改设置页或改提示词时一并核一遍。
- **词表判不了的仍归人审**：复述代码、过期断言、`##` 分节长文这三条要读懂上下文才成立。

`ROADMAP.md` 与 `docs/plans/*` 不在扫描范围内：按 D1 它们是执行记录，
第一人称与经过正是它们要留的内容。

---

# C　判断做完没

## C1　什么算「做完」

同时满足三点：

1. **有生产者也有消费者。** 协议里有类型、schema 里有列、界面里有渲染，
   但没有任何代码往里写，**不算做完**。（这条最容易自欺，本仓已经为此付过四条死链路。）
2. **失败路径有终态**，不能静默 no-op。空 `case` 直接 `return` 比落到 `default` 更糟
   ——它看起来被处理了。
3. **有回归测试锁住**，且测的是行为不是调用次数。

## C2　门禁

**一条命令，四道闸，全绿才算过：**

```bash
bun run gate
```

它按顺序跑：`typecheck`（`packages/*` 的 solution build **加** `apps/web`）→
`biome check` → `bun test` → `cargo check`（桌面端 Rust）。

`bun test` 这一闸里有两道结构守卫，红了不是「测试挂了」而是「规则被破了」：
`scripts/dependency-graph.test.ts`（B6 的包依赖方向）与
`scripts/comment-style.test.ts`（B10 的注释文体词表）。

**门禁只读，不改文件。** `gate` 里用的是 `biome check`，不是 `--write`——
要自动修跑单独的 `bun run check`。原因：门禁会在别人也在改这个工作区时跑，
一个顺手改写文件的门禁会动到不属于本次改动的东西。

**每次都跑全量，不设范围限制**——本仓全量是秒级，圈范围只会漏掉跨包回归。

类型档位见 `tsconfig.base.json`，lint 规则见 `biome.json`，两处是唯一事实来源。
只提醒最常踩的：`exactOptionalPropertyTypes` 开着，`{ x?: number }` **不接受**
`x: undefined`，只能把键删掉。

---

# D　文档、记忆与沟通

## D1　三份文档各管一段

| 文件 | 只写 | 不写 |
|---|---|---|
| `README.md` | 对外用法：怎么装、怎么跑、配置长什么样 | 为什么这么设计 |
| `ARCHITECTURE.md` | **为什么这么做**，每条附实测数据或踩坑记录 | 代码结构（读代码即可） |
| `ROADMAP.md` / `docs/plans/*` | 还没落地的，以及落地后的执行记录 | 已完成能力的说明 |

- **没有依据的偏好不写。** 结论被实测推翻时当场改写，不要留旧结论加一句「但是」
  （`ARCHITECTURE.md` 第 2 节把「Tauri 内存减半」整段划掉重写，是这条的样例）。
- **不写会过期的数字。** 「N 个测试」「M 个文件」写进文档的第二天就是错的
  ——要么不写，要么写「跑 `bun test` 看」。带日期的实测数据是证据，不受此限。

## D2　版本号只准出现在有真源的地方

- **发布清单的 `version`** —— 真源是根 `VERSION`，只能由 `bun run scripts/sync-version.ts`
  灌。改版本只编辑 `VERSION` 一行，**禁止手改任何清单**；`--check` 用于门禁。
- **落盘数据的 schema 版本** —— 就地定义 `<域>_SCHEMA_VERSION`，按它写进磁盘的键命名。
  **不设中心登记表**——中心表就是第二本账。
- **协议契约版本** —— 后端为真源，前端只声明支持上限。

禁止出现在：源码文件名 / 目录名 / 类名 / 函数名；测试文件名（除非测的就是某条迁移）；
文档文件名（用日期前缀）；计划文档的阶段号批次号（`_v2`、`批 B.4` 对读代码的人零信息量）。
`new` / `old` / `final` / `fixed` 这类相对时间词**任何位置都禁止**。
已落盘的键名是历史事实，一律不改。

## D3　记忆分两层

- **只跟 qywork 这份代码有关的** → `.claude/memory/`，每条一个文件 + `MEMORY.md` 索引。
- **整台机器都成立的**（Windows 编码陷阱、Playwright 不可用、npm shim）
  和**跨项目工作偏好** → 全局 `~/.claude/projects/.../memory/`。

分界理由：这台机器上还跑着别的仓库，机器级陷阱写进 qywork 等于让那边重踩一遍。
写新记忆前先读对应 `MEMORY.md`。记忆是线索不是结论，适用 A5。

## D4　回复短、单线

默认几句说完，能一行不两行。不把同一结论换几种表述再讲一遍，不写验收小作文，
不未问先展开「可能 / 可选 / 对比 / 背景」。**先做后说**——该改代码就改，
别用长回复代替干活。

---

# E　安全边界

- **明文 API key 不出服务端。** 读接口只回 `hasApiKey` 布尔；写接口收到
  `hasApiKey: true` 而没带 `apiKey` 时沿用旧值，不因客户端没回传就把 key 抹掉。
- **校验先于落盘。** 配置写接口不合法返回 422 且**不落盘**，不写一半。
- **只接受本机已存在的路径。** 不做 `git clone <任意 URL>` 式安装——那等于从网上取
  一段代码、下次加载就跑它。沙箱管的是插件能碰什么，不管它是不是你想要的东西。
- **路径参数拒绝含 `..` / `/` / `\` 的 id**，重名返回 409，**不静默覆盖**。
- **「完全访问」就是全部权限，路径边界也归它管。** `full` 的定义是「不裁决」，
  所以路径层跟着一起不设（`resolveInWorkspace` 的 `unrestricted`）。
  **只放开权限闸而留着路径层是错的**：同一个模式下 `run_command` 已经全放行、
  shell 里一个 `cd` 就出得去，于是变成「`read_file` 被拒、`run_command` 读到」的
  两套账——账本里真发生过（会话 `cv_0msw3jst9`，模型转头用 shell 读到了同一个文件，
  全程没告诉用户）。也不新增暴露面：`full` 下能用 shell 读到的，本来就一样能读到。
- **模式内的边界必须两侧同宽。** 文件工具与 shell 用**同一份根目录清单**：
  `auto` 下文件工具走 `resolveInWorkspace`、shell 走 `policy.ts` 的
  `literalOutsideHome`，两处都从 `workspaceRoot + additionalDirectories` 取。
  加新边界前先问「shell 那侧拦不拦得住同一件事」——只拦得住一侧的边界不是边界，
  是给模型指了条绕路。
- **凭证剥离与模式无关。** `scrubEnv` 两种模式一视同仁——它不是裁决，
  是「明文 key 不进子进程」这条事实。`~/.qywork/`（明文 key、权限模式、会话历史）
  在 `auto` 下由「工作区外路径」与「凭证文件」两条硬规则挡着；`full` 下不挡，
  那是这个模式的定义。
- **工作区里的 `.agents/` 在 `auto` 下由文件工具拦，shell 有意不拦**：
  `PROTECTED_DIRS` 挡住 `write_file` / `edit_file`；shell 那侧**不加**
  「写它等于自我提权」这条，理由写在 `policy.ts` 里——它拦不到任何能力
  （模型手里已有 `run_command`，MCP server 本身就是它能直接启动的进程，
  给自己加个工具没有获得新能力），而 `.agents/memory/` 本来就该由 `memory` 工具写，
  shell 拦就是两套账。`full` 下这一层同样不设，理由同上。
  它与 `~/.qywork/` 名字像，位置和含义完全不同，别混。

---

# F　明确不采纳

- **「禁止跑全量测试，只测影响范围」**——那是给跑一次很久的大仓定的。
  本仓全量是秒级，圈范围只会漏跨包回归，收益是负的。见 C2。
- **「一切只在 master 直接提交、禁开分支」**——那来自并行会话很密的场景，
  qywork 没有这个约束，按常规做法走。
- 但**「带 pathspec 点名提交、禁止 `git add -A`」值得采纳**：
  不带 pathspec 的 `commit` 提交的是整个 index，会卷走并行会话暂存的内容。
