# qywork 架构决策

这份文档记录**为什么这么做**，不记录代码结构（代码结构读代码即可）。
每条决策都附实测数据或踩坑记录；没有依据的偏好不写进来。

---

## 1. 拓扑：qy CLI 是内核，桌面端只是它的一个前端

```
Tauri(Rust) ──spawn──> qy serve (Bun 单文件二进制)
     │                      ├── http://127.0.0.1:<随机端口>
     └── WebView ───────────┤   ws://.../stream
 手机浏览器 ─── 局域网 ──────┘   扫码 = URL + 配对令牌
```

`qy` 是发布产物本体，多模式共用同一份实现：

| 模式 | 用途 |
|---|---|
| `qy exec "..."` | 单次执行，`--json` 出 JSONL 供 CI |
| `qy serve` | 本地 HTTP + WebSocket，桌面端与手机端都连它 |
| `qy`（默认） | 交互式 TUI（未接线） |
| `qy mcp` / `qy team run` | 未接线 |

**Tauri 侧零业务状态**：外壳一旦自己揣一份数据库或缓存，就和 `qy serve` 成了
两本账，而两本账迟早漂移、漂移了很难发现。宁可多一次 localhost 往返。

**WebView 不走 Tauri IPC 拿业务数据**，它直连 `qy serve` 的 WebSocket，与手机端
完全相同的协议。所以「桌面能做手机做不了」的能力漂移在结构上不存在。

---

## 2. 桌面壳选 Tauri 2 —— 但对 Windows 的体积/内存论证需要更正

最初的论证是「Tauri 装机包 8~15MB、内存约为 Electron 的一半」。**实测后必须更正**：

### 内存（Windows 实测，本机 2026-08）

| 进程 | 内存 |
|---|---|
| qywork 主进程（Rust） | 29.3 MB |
| qy sidecar（Bun） | 63.8 MB |
| WebView2 × 6 | 314 MB |
| **合计** | **436 MB** |

**Windows 上 Tauri 用的 WebView2 就是 Chromium**，所以内存与 Electron 基本持平。
「内存减半」是 **macOS 独有**的（那里是 WKWebView），不适用于 Windows。

### 体积（实测）

| 组件 | 未压缩 | 压缩后 |
|---|---|---|
| qy sidecar（Bun 单文件） | 94.8 MB | 37.1 MB |
| Tauri 外壳 | ~10 MB | — |

Bun 单文件二进制内嵌整个运行时，这是 94.8 MB 的来源，换 Node SEA 也是同量级。

### 结论：Tauri 仍然是对的，但理由变了

真正成立的优势：
1. **不随包分发 Chromium**。WebView2 在 Win10/11 已预装，macOS 的 WKWebView 是系统组件。
   安装包 ≈ 45–50 MB，Electron ≈ 85–100 MB。
2. **macOS 上内存优势真实存在**（WKWebView）。
3. Rust 侧承担 PTY、文件监听等 Web 干不了的事。

不成立的：Windows 上的内存优势。**不要再用「内存减半」说服自己或别人。**

---

## 3. PTY 在 Rust 侧，不在 Bun sidecar 里

Bun v1.3.5 起内建 PTY，但**只支持 POSIX，Windows 没有 ConPTY 实现**。
Windows 是主开发平台，所以交互式终端由 Rust 的 `portable-pty` 承担。

由此划出一条能力边界：**手机端没有终端**。PTY 在 Tauri 进程里，网络那头够不着，
所以握手的能力声明里 `pty: false`——与其让手机上出现一个点了没反应的入口，
不如不显示。

agent 自己执行命令走的是 sidecar 里的管道（`Bun.spawn`），与此无关。

---

## 4. 前端选 Solid

长 transcript + token 级流式渲染是这类 UI 最痛的地方。Solid 的细粒度响应式让
「模型每吐一个 token 只更新一个文本节点」成为默认行为，不需要给列表做 memo 化。

实测产物：首屏 111 kB（gzip 37 kB），highlight.js 162 kB 拆成独立 chunk
按需加载——大多数会话的头几屏没有代码块，不该为它付首屏成本。

---

## 5. 已经踩过并修掉的坑

这些都是实测撞出来的，不是理论风险。每条都有回归测试锁着。

### 5.1 ToolContext 每波次重建（致命）

`AgentLoop` 曾在每个执行波次重建 `ToolContext`，`state` 是新 `Map`，
因此 `read_file` 记录的「本轮读过哪些文件」立刻丢失，写入守卫把刚读过的文件
判成没读过。

**真正的教训不是「状态丢了」**：写入工具连续被拒后，模型没有放弃，而是改用
`run_command node -e "fs.writeFileSync(...)"` **绕过守卫手写文件**，产出了
BOM + 只有 CR 换行的坏文件。**守卫写错比没写更危险**——它会把 agent 推向没有
任何守卫的那条路。

修复前 18 轮 17 次工具调用 5 次失败；修复后 4 轮 3 次调用 0 失败。

测试：`packages/agent/src/loop.test.ts`

### 5.2 ID 不单调（地基级）

`monotonicId()` 只把毫秒时间戳编进前缀，**同一毫秒内的后缀是纯随机的**。
注释里写着「保证按字典序排序≈按创建时间排序」，但实现只做了一半。

击穿两处：会话列表排序；以及 `listMessages` 用 `id <= upperBound` 划定的
run 消息高水位——同毫秒写入的前序消息会被判成「在水位之后」，**直接从历史里丢掉**。

现在是三段定宽 `<时间戳><同毫秒序号><随机尾>`。定宽同样必需：变长时 `z9` 会排在
`aaa` 之前。

测试：`packages/core/src/domain/ids.test.ts`

### 5.3 计费口径混用

OpenAI 兼容协议的 `prompt_tokens` **含**缓存命中（DeepSeek 实测
`prompt_tokens = hit + miss`），Anthropic 的 `input_tokens` **不含**。
两边同一套累加逻辑，兼容侧会把命中的 token 按全价再算一遍——缓存命中率越高，
账单偏差越大。现已统一收敛到排他口径。

### 5.4 局域网地址挑错（二维码印了连不上的地址）

先后错了两版：
- 按网卡名黑名单排除虚拟网卡 → `singbox_tun` 和真实网卡打成平手，靠枚举顺序决定胜负；
- 问内核默认路由（UDP connect 8.8.8.8）→ **VPN 下默认路由指向 TUN**，选中了代理网卡。

第二次的成因更典型：**路由表回答的是「本机怎么出去」，而这里要的是「手机怎么进来」**，
装了 VPN 的机器上这两者不是同一块网卡。

现在用三个结构性信号叠加（全零 MAC / 虚拟化厂商 OUI / 掩码 /24 + 私有网段），
**并且始终把完整候选列表交给用户**——自动判断在 VPN 环境下没有可靠解，
兜底才是真正的保障。

测试：`packages/server/src/pairing.test.ts`

### 5.5 sidecar 孤儿进程

外壳的退出清理只在正常退出路径上跑，崩溃或被强杀时（实测 `Stop-Process` 就会）
不触发，留下的 `qy` 占着端口和 SQLite 的 WAL 锁，**下次启动直接起不来**。

改成 sidecar 自己盯父进程（`--parent-pid` + `kill(pid, 0)` 轮询），
跨平台且覆盖崩溃场景。实测强杀父进程后 sidecar 自行退出。

### 5.6 `**/24` 把块注释提前闭合

JSDoc 里写 `**/24 掩码` 时 `*/` 会闭合注释，整个文件从那里开始变成语法错误。

### 5.7 同一个常数抄三份，然后漂了

`estimateTokens` 在三个 provider 里各写了一遍。合并时才发现
`openai-responses.ts` 用的是 **÷4**，另外两个是 **÷3.5**——
**同一个上下文占用面板，换个 provider 就差 14%**，而且没有任何人会想到
去那三个文件里对比一个私有函数。

一行的函数最容易被复制：抄它比 import 它少一步。但它一旦承载了**口径**
（这里是「多少字符算一个 token」），三份就是三个会各自演化的口径。
判据不是行数，是**它有没有回答一个必须全局一致的问题**。

现在合成 `packages/ai/src/tokens.ts` 一份，测试里断言「同输入同输出」——
那正是三处各自实现时不成立的那条。

拿同一把尺子扫过一遍全仓之后，又找出两组，**扫法值得记**：

- **按名字扫**（同名 const 出现在多个文件）捞到十组，其中八组是 CLI 的 ANSI
  色码——那些不会漂也漂不出后果。
- **按值扫**（同一串字面量出现在多个文件）才捞到真的：噪音目录清单
  （遍历工作区时跳过哪些目录）抄了三份，**已经漂成 13 / 12 / 11 条**。
  **名字不同的那份只有这把尺子能捞到**——`IGNORED` 和 `IGNORED_DIRS`
  是同一个概念的两个名字。

那次漂移里真正露出来的只有 `coverage` 一条（其余差异被各自的「点开头一律跳过」
规则挡住了，没有可观察后果）。但它的后果很典型：**三方对「这个目录存不存在」
给出三个答案**——用户在文件树里看不到它，`grep` 搜不进去，`list_dir` 却把它列出来。
模型据此去读一份构建产物当源码。

另一组是 effort 档位那五个字面量，写了六遍、横跨五个包，**还没漂**。
它值得动的理由不是整洁，是**编译器只挡得住一个方向**：某处多出一档会在赋值处报错，
少一档不报错——它只是表达不出来。真实的失败形状是「以后加了一档，
服务端认、界面选不出来」，安静地少一个能力，没有任何红灯。
合并时派生方向是**数组 → 类型**（`EFFORT_ORDER` 在前，`EffortLevel` 由它派生）：
类型只在编译期存在，而探针要逐档试、适配器要按序比大小，两处都需要一个
运行期能枚举的数组；反着写就必然再抄一份出来。

**一处刻意不合并**：`catalog.ts` 里 `CLAUDE_BASE.effortLevels` 保持字面量。
它不是「档位全集」，是「实测 Claude 这五档都支持」的**事实声明**——
改成引用 `EFFORT_ORDER` 等于替以后新加的档位替 Anthropic 作保。

### 5.8 CSS 转义写箭头，被当成八进制

想在 `content` 里画一个 `›`，写成了「反斜杠 + 203a」这种 CSS 十六进制转义。
这段字符串经过一层字符串处理后，前三位被当成**八进制**解释成 `0x83`，
落到文件里是一个不可见字节 + 字面量 `a`，页面上显示为一个豆腐块加一个 a。

**同一个坑在同一轮里踩了三次**：折叠箭头、计划卡箭头，
以及**写这条记录本身**——把转义序列贴进文档时它又被解释了一遍。

结论：**CSS 里的符号一律写字面量字符**，不用十六进制转义。
可读性上没有任何损失，而一个转义序列要正确穿过多少层字符串处理是不可控的；
连「描述这个问题」都会再触发一次，足以说明它有多容易复发。

---

## 6. 不要"优化"掉的不变量

每一条都是踩出来的：

- **一次工具调用 = 一行 tool_action**，原地从 running 更新到终态；没有
  tool_call / tool_result 两行。
- **`execution_started_at` 是崩溃恢复的歧义边界**：进入执行器前单独提交。
  有它 = 可能已执行（不可重放）；没有 = 确定没执行。
- **`cachedTokens: null` ≠ 0**。null 是「provider 未回报」，0 是「真实零命中」。
  混同会让「缓存从来没生效」看起来像「缓存生效但没命中」。
- **冻结前缀逐字节稳定**：日期、技能清单、记忆**永不进前缀**，一律压到 transcript 之后。
  工具 schema 按名排序（工具渲染在最前，顺序抖动即全量缓存失效）。
- **废除静默完成**：`stopReason` 必填，前端要能回答「它为什么停了」。
- **fail-closed**：注册表未命中的调用返回结构化失败且 `executed=false`，绝不伪装成功。
- **消息高水位**：run 创建时定格 `messageIdUpperBound`，排队期间新增的消息
  不得穿越进本 run 的历史。

## 7. 渲染层已落地的形状

逐条核过，现已全部接上生产者与消费者：

- Markdown（GFM + 高亮，流式期关 auto-detect，定稿后重渲染并开启）
- 工具组卡：连续工具折叠成一张，只被正文打断；thinking 不切组，
  夹在首尾工具之间的进组、组前组后的单独成条
- 思考标签态：流仍在增长说「思考中」，停了说「已思考」
- 失败卡默认展开
- 压缩事件卡（started/done/failed 三态，失败时醒目）
- 被重试接替的 run 降透明度保留（不折叠、不隐藏——那些步骤是真实发生过的历史）
- RunStatusBar：停止原因恒显示 + 真实用量（缓存未回报显示「未回报」而不是 0）

> **这份清单曾经说过一次假话。** 「被重试接替的 run 降透明度」那条在写下时
> 渲染代码确实存在，但 `superseded_by` 从来没有写入方，那段分支永远不会执行。
> 见第 11 节——这是本项目最容易重犯的一类错误。

---

## 8. 前端体积纪律

首屏 118 kB（gzip 40 kB）。三块重的资源全部按需加载，各自有明确触发点：

| 模块 | 体积 | 何时加载 |
|---|---|---|
| highlight.js | 162 kB | 出现第一个代码块 |
| SidePanel（含 CodeMirror） | 222 kB | 打开文件/变更面板 |
| qrcode | ~28 kB | 打开手机接入 |

两次踩过同一个坑：**vite 的 `manualChunks` 会把动态 import 的模块合并回同一个
chunk**，按需加载就白做了（实测 CodeMirror 语言包被合成 593 kB 一坨）。
现在交给 Vite 按动态导入边界自动切分，不手工归组。

---

## 9. 局域网监听必须换端口

`0.0.0.0:P` 与已绑的 `127.0.0.1:P` 在同一端口冲突，直接报
「Failed to start server. Is port P in use?」——实测撞到过。

所以「允许手机接入」时追加的监听器传 `port: 0` 让内核挑空闲端口，
二维码指向那个新端口。两个监听器共用同一份 handler / bus / store，
手机连上看到的是同一份状态。

之所以不重启服务改绑 `0.0.0.0`：重启会断掉桌面端的 WebSocket、丢掉正在跑的 run。

---

## 10. Windows 网卡名乱码

Windows 上非英文网卡名（「以太网」）经 `os.networkInterfaces()` 取出来时，
UTF-8 字节被按 Latin-1 逐字节解码，显示成 `ä»¥å¤ªç½`。用户要靠这个名字辨认
该选哪块网卡，乱码等于配对功能作废。

`repairMojibake()` 用严格 UTF-8 解码做判据还原——严格模式很关键，
它保证不会把本来就正常的西欧文字名（`Café`）误改。

---

## 11. 最容易重犯的一类错误：协议里有类型 ≠ 有实现

这是本项目迄今为止**唯一反复出现**的错误模式，单独立一节。

写协议时把类型定义齐全是好习惯，但它有一个副作用：**类型定义看起来像实现**。
类型在、schema 列在、读取路径在、渲染代码在——唯独没有生产者。
因此文档照着类型写，UI 照着类型渲染，整条链路看起来都在。

在批次 A 里一次性挖出六条，全部是同一个形状：

| 死链路 | 有什么 | 缺什么 | 表现 |
|---|---|---|---|
| `run.started` | 事件类型、前端 handler | **任何发送方** | 前端拿不到真实 runId |
| `run.retry` / `superseded_by` | schema、读取、投影、渲染、CSS | 写入方 + 指令处理 | 文档声称的降透明度永不触发 |
| `conversation.setModel` | 指令类型、`conversations.model` 列 | 一个空 `case` 假装处理了 | 切换静默失效，按新模型计价 |
| `conversation.compact` | 指令类型 | 处理分支 | 被 `default` 吞掉 |
| `todos` | 事件类型、`TodoItem` | 发送方 | 面板永远空 |
| `capabilities.plugins/teamBackends` | 字段 | 枚举逻辑（硬编码 `[]`） | 能力探测恒为假 |

后来又补上三条，形状相同但**发现方式不同**（见 §20.1、§25、§26）：

| 死链路 | 缺什么 | 怎么被发现的 |
|---|---|---|
| `Session.dispose()` | **一个调用点都没有** | 接 MCP 时算子进程数量对不上 |
| `state.capabilities`（前端） | 存了，没有任何组件读 | 加 `mcpServers` 字段时一并查消费者 |
| `stream_idle_timeout` | 任何发送方 | **冒烟脚本超时**——它是唯一一条「事件不出现」才成立的 |
| `additionalDirectories`（第一次） | 类型在、体检在，**没有一处据它放开路径** | 是我自己加的。发现方式：改完配置后回头 grep 自己刚加的字段 |
| MCP 握手的 `capabilities` | 收到了，**原地丢掉** | 想做 `resources/*` 时才发现它从来没被留下来 |

后两条与前面几条有一个区别值得写下来：**它们都是「我这一轮刚加的」**。
死链路最容易在**新增字段的那一刻**产生，而不是在维护中腐化——
因为写类型和写消费者之间隔着几个文件，中途很容易觉得「这条等下再接」。
`additionalDirectories` 那条当时的正确处置是**删掉**，
直到真的有场景要它、且三层全部接上为止（§31）。

MCP 那条的表现最值得记：一个只提供 `resources`、不提供 `tools` 的 server
会**连上、握手成功、注册 0 个工具、不报任何错**。用户看到「配了但什么都没发生」，
日志干干净净。「丢掉一个暂时不用的字段」看起来无害，
而它是那条现象唯一能被解释的地方。

### 2026-08 又两条，其中一条是**新形状**

界面返工时挖出来的：

| 死链路 | 有什么 | 缺什么 | 表现 |
|---|---|---|---|
| `messages.attachments` | 类型、DB 列、写入函数、**三个 provider 的图片编码器** | 服务端不转发、落库不带、装配历史时丢掉 | 类型齐、编码器齐，**中间三处把数据丢在地上** |
| `ContextEvent.breakdown` | 类型、**生产者**、`_group` 标注 | 生产者写死七个 0；消费者一个都没有 | 见下 |

**`breakdown` 是一个此前没见过的形状：有生产者，但生产的是假值。**

前面所有条目都是「没有生产者」。这一条不同——`loop.ts` 确实每轮都发 `breakdown`，
七个字段全填 0。它比没有生产者更坏：

- 「没有生产者」在接消费者的那一刻就会暴露（拿不到数据）；
- 「生产假值」接上消费者之后**看起来完全正常**——面板会画出一个结构完整、
  数字全零的饼图，而没有任何人能从界面上看出那是假的。

而分组信息一直是真实存在的（`session.ts` 标 `historyMessages`、
`loop.ts` 标 `executionRecords`、`compaction.ts` 标 `summary`），
**只是从来没有人按组去量**：这条链路铺了三段，停在最后一步。

处置：填真值（`breakdownOf(req)` 按 `_group` 分桶），而不是删字段——
三段路都铺好了，删掉等于把它们一起废掉。

**新增一条判据**：
- **有生产者不等于生产的是真值。** 检查死链路时，除了问「谁发的」，
  还要问「发的是不是恒定的占位值」。全 0、空数组、空字符串这类
  「结构对、内容假」的产出，要按死链路同等对待。

### 2026-08 又两条，以及**扫法本身的教训**

按字段名逐个扫全仓找出来的（判据：除了声明处，还有没有别的地方提到它）：

| 死链路 | 有什么 | 缺什么 | 表现 |
|---|---|---|---|
| `TeamMemberEvent.childConversationId` | 声明、**服务端已经算出来并返回了**、端口类型也带着 | 编排器接到返回值后只用 `ok/output/error`，把它丢了 | 成员到底读了什么、跑了哪些命令永远看不到 |
| `capabilities.fileWatch` | 字段、生产者 | 全仓没有任何文件监视器，值写死 `true` | 无人读，所以不报错；但握手里掺了一句假话 |

第一条与 `messages.attachments` 同形——**两头都接好，中间那节把值丢在地上**。
这种断法比「从头到尾都没有」更难发现：每一头单独看都是对的。

第二条是「生产假值」的第二个实例，而且**发生在能力位上，比在数据上更坏**：
握手是「这一轮跑在什么边界里」的唯一答案，答案里掺假的那一句，
下一个据它做判断的人会被骗一次。处置是改成 `false`——B5 的口径本来就是
「能力不存在时握手里声明 `false`」，旁边的 `pty: false` 就是这么写的。

**扫法的教训有两条，比这两个发现更值钱**：

1. **按「有没有 `.字段名`」判消费侧会大批误报。** 第一遍这么扫，把
   `contextPercent` 这类**经由整个对象走 API 出去**的字段全判成了死链。
   正确判据是「除了声明处还有没有别的地方提到它」，然后逐个开文件核。
2. **「部分覆盖」启发式的信噪比很低。** 把所有字面量联合抽出来、逐个成员去界面里找，
   命中十五个「只认一半」，逐个核完**只有一个是真的**（`protocol_mismatch`
   导致的无限重连，见 §34）。四个是查法本身的假象——`stopReasonLabel`
   是一个键不带引号的 `Record`，按字符串搜根本搜不到。
   不逐个开文件核，这一轮会产出十四条假优化项。

### 连锁效应比单条更值得警惕

`run.started` 没有发送方，直接导致 `interrupt()` 拿 **step id 当 run id** 发出去
（前端只能从 transcript 里找 `status === 'running'` 的条目）。服务端查不到就静默返回，
**中断按钮从来没生效过**，而 UI 上完全看不出区别——点了，转圈继续转。

一条死链路会长出第二条：下游代码只能用它拿得到的那部分数据。

### 结论

- **每条协议成员必须能指出它的生产者和消费者**，指不出就是死链路。
- 端到端冒烟脚本要断言**具体事件出现过**，而不只是「run 跑完了」。
  批次 A 的六条里，有五条是加了断言之后当场暴露的——单测和类型检查一条都抓不到。
- 未实现的分支**明确拒绝**（`command.rejected`），绝不静默 `return`。
- **断言「X 出现了」抓不到「什么都没出现」。** 冒烟脚本的超时本身就是一条断言——
  它响了就说明有路径没有终态，不能因为重跑一次绿了就放过（`stream_idle_timeout`
  就是这么漏了一整轮的）。
- 公开方法有定义同样不等于有人调。`dispose()` 写好了却零调用点，
  表现是每条消息泄漏一套子进程。

## 12. 上下文超限与输出截断是两件事

`max_tokens` 这个词在 4xx 错误里绝大多数指**输出**上限的参数校验
（`max_tokens must be ≤ 8192`），不是输入超限。曾经 `errors.ts` 用
`message.includes('max_tokens')` 判 `context_overflow`，`loop.ts` 用
`providerStop === 'max_tokens'` 判 `context_exhausted`——两处都反了。

后果不只是文案不准：容量判定是**压缩并重发**的触发器。判宽了，一个普通的参数错误
会触发压缩，压缩本身要花一次模型调用，压完重发还是同样的参数错误 ——
**烧钱的死循环**。

所以 `classifyCapacityRejection()` 是一个窄判定，宁可漏判也不误判：

- 只认 400 / 413 / 422；429 即使消息里带 context 也不算，5xx 更不算
  （服务端故障压缩了也没用）
- 要么命中 provider 原生容量码，要么消息强匹配；强匹配的**第一道闸是「输入轴」**——
  消息里必须出现 context / prompt / input token，`max_tokens must be ≤ 8192` 一个都没有
- 泛化的 `invalid_request_error` 只作日志记录，**不作判据**

`StopReason` 相应拆成两个值：`context_exhausted`（说不下了，要精简上下文）与
`output_truncated`（听不下了，要加大 max_tokens 或让模型分段）。用户的下一步动作
完全不同，混成一个值会把人引向解决不了问题的那条路。

## 13. 崩溃恢复的歧义边界在 step 上，不在 run 上

`runs.execution_state` 这个列存在但**从未被写入**。拿它做恢复判据会让所有残留 run
都被判成「安全可重放」——正好是最危险的那个方向。

真正的边界是 **step 的 `execution_started_at`**：存在「非空但 status 仍是 running」
的 step，说明那个工具进了执行器却没落终态，**可能已经跑完并产生副作用**，
也可能刚进去就崩了，两者无法区分，所以整轮结果不可信。

配套三条：

- 两种情况的 `stopReason` **必须不同**（`internal_guard` vs `user_interrupt`），
  统一了就无法区分「进程崩了」和「用户点了停止」
- 卡在 running 的 step 一并落终态，否则 UI 留一张永远转圈的工具卡
- 孤儿 step 的 `executed` 取**保守值 true**：无法判定时不能向模型断言「没有副作用」

不做这件事的后果不是「界面不好看」：残留的 running run 会让 `isBusy` 恒为真，
那个会话**永远发不出新消息**。

## 14. 中间资源：判据是「不可重放」，不是「大」

上下文能力的第一层。两张工具名表把判据钉死：

- **本地权威**（`read_file` / `grep` / `list_dir` …）：完整正文就在工作区里。
  历史被压缩掉之后模型再读一遍就是了。**这类不落盘**，哪怕它 10 MB——
  落一份盘只是把同一份数据存两遍。
- **内容权威**（`run_command` / `web_fetch` / `web_search` / `mcp__*`）：字节**无法重建**。
  那个网页此刻抓到的样子、那条命令这一次的输出，过去了就没了。这类必须先落盘。

按「超过 N KB 就落盘」判会同时犯两个方向的错：把可重放的源码存成副本，
又漏掉不大但不可重建的输出（一次 200 行的 web_search 结果）。

`mcp__` 前缀一律按不可重放处理——第三方工具的可重放性本地无从判断，保守假设。
这个判错方向是不对称的：**误判成可重放会永久丢信息，误判成不可重放只是多存一份。**

### 曾经的做法是直接丢

`run_command` 原本超过 60000 字符就把中间**永久丢掉**。命令输出不可重放，
那条 `npm test` 这一次的失败详情丢了就是丢了，模型只能凭首尾猜，或者让用户再跑一遍。

### 两个库，顺序是硬约束

正文在 `qywork_content.sqlite3`（内容寻址、分片、可 GC），事实在主账本的
`intermediate_resources` 表。跨库没有外键，所以：

> **先在正文库定稿 blob，再往账本登记。**

反过来做会让账本指向不存在或不完整的正文，而那种损坏要到模型来读的时候才发现，
届时原始字节已经没了。回归测试 `runtime/sink.test.ts` 专门锁这条：
正文库写失败时账本里必须**一行都没有**。

### 覆盖事实必须交给模型

投递给模型的是摘要 + `resource_id` + **覆盖事实**（投了多少 / 一共多少 / 截没截）。
少了覆盖事实，模型会把 8 KB 摘要当成 206 KB 的全部，然后基于不完整信息下结论——
**比不给它更糟，因为它不知道自己不知道。**

### query 投影不是可选项

第一版 `read_resource` 只有字节偏移。实测让模型找「第 2000 行」时，
它连发**五次** read_resource 手工二分猜偏移，每次都是一轮完整的模型往返。
补上 `query`（扫全文返回命中行 + 行号 + 字节偏移）之后一次就够，
同一任务输出 token 从 1427 降到 1020。`query` 看着像锦上添花，实测才知道是必需品。

## 15. 又一次孤儿进程，这次在截图脚本里

`shoot-ui.mjs` 在 Windows 上用 `shell: true` 拉起服务，因此 `proc` 是 cmd.exe，
`proc.kill()` 杀的是壳**不是 bun 子进程**。留下的服务占着 SQLite 的 WAL 锁，
下次跑脚本清工作区时报 `EBUSY: resource busy or locked`。

修法与第 5.5 节完全相同：传 `--parent-pid`，让服务自己盯父进程。
**同一个坑在不同地方会再长出来**——凡是 spawn 长驻子进程的地方都要过一遍这条。

## 16. 上下文压缩：发送前触发 + 投影，且必须验保真度

### 触发只有一个入口：发送前按占用判

`agent/loop.ts` 在装配完请求、发出去之前拿占用比软阈值，超了才压。

不做「发出去被 provider 拒了再压、然后重发」：那个形状每次触发都要先烧掉一次
注定失败的长请求，长 prompt 上是几秒到几十秒外加计费。反对阈值的常见论据是
「本地估算永远不准」，那个前提在锚定尺下不成立——占用 = provider 真值 + 仅一轮
尾巴的估算，误差被限制在单轮增量内。

容量拒绝那条路仍然分类（`classifyCapacityRejection`），但**不触发压缩**，
只如实报错：两个触发就是两个执行入口。手动压缩（`conversation.compact`）
共用同一个 `RuntimeCompaction`，只是判据换成用户的显式意图。

代价说清楚：估算失误时不再自动救回，直接报错。可接受——单次/单批投递预算
给了跳变上界，不存在无预警的跃迁。

### 压缩是投影，不销毁数据

产出一份 manifest，构造请求时按它投影；**Message / Step / 正文库一个字节不动**。
所以压缩可撤销、可重放、历史面板永远显示完整会话。做成「删掉旧消息换成摘要」的话，
用户翻历史会发现前面的对话凭空消失。

### 事实包不经模型

文件路径、用户约束这类事实一旦被摘要改写就不再可靠，而模型后续会拿它们去改文件。
所以 `facts` 由机械提取产出，与摘要正文分开成条投递——拼进自由文本会被下一轮压缩
再次改写，路径经不起两轮改写。

### 摘要失败要有降级

摘要本身是一次模型调用，而它是在上下文已经超限时发的，更容易失败。
`localSummary()` 是不调模型的确定性降级：质量差得多，但「差的摘要」远好于
「压缩失败 → 请求还是超限 → run 直接失败」。

### 保真度必须实测，单测测不出来

单测能测「触发了压缩」「manifest 结构正确」，**测不出「压缩后模型还记不记得关键信息」**——
而那才是压缩的全部价值。所以有 `scripts/compaction-fidelity.ts`：
造 40 轮会话、埋 5 条可判定事实、压缩后逐条问真实模型。

**它当场抓到一个自测发现不了的 bug**：第一版压缩后的投影是原文的 **213%**——
压缩把上下文变大了。两个原因：`userConstraints` 逐字保留了全部 39 条用户消息
（等于原样复制），摘要预算是固定 4000 字符不随输入缩放。

修法：预算改成输入的 25%（带上下限），用户消息只有**带约束的**才逐字进事实包。
结果 213% → 62%，5 条事实全部保真。

判据用三类模式，缺一类就丢事实：否定与命令、赋值型约定、**带单位的数字**。
最后一类是实测补上的——「令牌有效期定为 15 分钟」不含任何关键词，
而模型概括时最容易丢的就是具体数值。

## 17. 插件必须进程隔离，不能靠免责声明

同进程加载下，插件能直接读宿主的 `process.env` 拿走 API Key，也能拿到宿主的
工具注册表、sink 句柄、权限回调这些进程内对象。

文档里写「只装你信任的插件」是**用免责声明代替安全边界**。插件生态的意义就是
装别人写的插件；要求用户先审计源码等于取消了这个生态。

所以现在是**子进程 + stdio JSON-RPC**：

- 不用 `worker_threads`——worker 与主线程共进程、共 `process.env`，隔离是假的。
- **不透传宿主环境变量**。`process.env` 里有 API Key、令牌、代理配置，
  插件进程本不需要它们，透传等于把凭证白送出去。（有回归测试锁这条。）
- 权限在**宿主侧**按 manifest 强制，方法名到权限的映射写死一张表，
  未登记的方法一律拒——fail-closed 的方向是「新能力用不了」，
  不是「新能力对所有插件无条件开放」。
- 插件的 `console.log` 会污染 stdout，所以**解析失败的行一律忽略**而不是当协议错误，
  否则一句调试打印就能把整个通道破坏掉。
- 进程崩溃时在飞的调用**逐个拒绝**，不留着挂到超时——超时对用户表现为「卡住」。

### 17.1 这道边界的范围被写夸大了，必须更正

上面这段一度写的是「插件拿不到 fs / net / child_process」。**实测是错的**：
起一个插件子进程，让它 `import('node:fs')` 读主目录、`import('node:net')`、
`import('node:child_process')`——三样全部成功，只有环境变量确实是干净的。

子进程隔离的是**宿主侧的资源**（环境变量、进程内对象、崩溃传播），
不是插件自己的系统调用。所以：

> **manifest 权限管的是「通过宿主做事」，不是「插件能做什么」。**
> 一个恶意插件不需要声明任何权限，就能读走 `~/.ssh` 并自己发出去。

这个错写得危险的地方在于，它让权限清单**看起来像一道沙箱**，用户会据此判断
「这个插件只声明了 read，装了没风险」——而那个判断是错的。权限清单的真实作用是
**知情同意**：让人在安装前知道插件打算干什么。

现在有一条**故意断言「没挡住」**的测试（`host.test.ts` 里那条 `node:fs 仍然可用`）。
哪天真上了沙箱，它会红，改它的人就必须同时更新这一节——否则文档会再一次
在无提示的情况下比实现更乐观。

真正的沙箱路径（Node `--permission` / OS 级 job object、seccomp、AppContainer）
没做之前不写成已做。

### 17.2 插件的 exec 不能透传环境变量

`run_command` 内置工具把 `process.env` 整个交给子进程——那是用户自己批准的命令，
看得到自己的环境天经地义。**插件的 `exec.run` 绝不能这样**：宿主费劲把插件进程的
env 洗干净了，如果它转手能 `echo $ANTHROPIC_API_KEY`，那道清洗就等于没做。

同一个「跑命令」的动作，在两个信任级别下必须是两份实现。共用一份会在某次重构里
被合并掉，而合并的方向几乎必然是「宽的那份」。

## 18. 技能与记忆：只有索引进上下文

技能 = `<层>/skills/<name>/SKILL.md`，记忆 = `<层>/memory/*.md`。**三层**：
内置（随程序发布，只读，用户不可见）> 项目（工作区 `.agents/`）> 全局（`~/.qywork/`），
同名先认领的赢。规律是「谁不可写谁最高」——内置压不住项目层的话，项目层就能静默
替换掉系统自己的行为。**项目层不叫「用户层」**：跟着人走的是 `global`，跟着仓库走的
才是这一层，两个词在别处含义相反。项目层用 `.agents/` 而不是 `.qy/`，是因为它是跨客户端约定
（agentskills.io），认这条约定的客户端都读得到；`~/.qywork/` 任何外部 CLI 都读不到，
这是三层之间唯一的实质差别。**解析只有一份函数**，加载器和设置页共用它——
两边各扫一遍的话，菜单描述的是一个技能、跑的是另一个。都是**普通文件**：
用户能直接看、直接改、直接删，能进版本控制，agent 用普通文件工具就能读。
存进 SQLite 就需要一套 UI 才能管理，而记忆是最需要随时纠正的一类数据。

**尾区只放索引（name + 一行描述），正文由模型按需拉取。** 全放正文的话十个技能
就能占掉几万 token，而一次任务通常只用得上一个。

两者都**永不进冻结前缀**——装一个技能、记一条记忆就会让整个 provider 缓存失效，
那个代价远大于它们省下的 token。这是第 6 节既有不变量的延续。

## 19. 出网工具的前置条件是 SSRF 闸

agent 拿**模型生成的 URL** 去发请求，而模型的 URL 来自它读到的网页内容。
不挡内网就是把 SSRF 直接递给模型。

三条容易漏掉的：

1. **重定向后必须重新校验。** 只查首个 URL 挡不住 `evil.com` 302 到 `169.254.169.254`。
   所以手动跟随重定向，每一跳都过闸。
2. **按解析后的 IP 判，不只看主机名。** `metadata.evil.com` 可以 A 记录指向元数据端点。
3. **IPv4 映射的 IPv6 地址要展开判**：`::ffff:127.0.0.1` 与 `127.0.0.1` 等价，
   不展开的话一个 `::ffff:` 前缀就绕过了全部 IPv4 规则。

另外 `dns.lookup()` 没有超时参数——DNS 被劫持或查不存在的 TLD 时会挂到系统级超时
（实测 5 秒以上），那段时间整个工具调用是卡住的。所以加了 3 秒超时，超时按解析失败处理。

### 19.1 手动跟随重定向 = 浏览器帮你做的事都得自己做

给 `safeFetch` 加上 `method`/`headers`/`body`（插件的 `net.fetch` 需要 POST）之后，
两件浏览器默认行为必须自己补上，否则新增的能力自带两个漏洞：

- **跨源跳转丢掉 `authorization` 和 `cookie`。** 不丢的话，任何能让你打一个 URL 的人
  都能把凭证钓走：请求 `api.example.com`（带 token）→ 对方回 302 到 `evil.com`
  → 凭证跟着过去。
- **`host` 头不允许调用方指定。** 闸按 URL 里的主机解析 IP，反代看的是 `Host` 头，
  两者不一致就能把「校验过的 IP」和「实际访问的服务」掰开。逐个逐跳头
  （`connection`/`transfer-encoding`/…）同理，头值里带 CR/LF 的整条剔除。

还有一条不是安全而是正确性：301/302 上的非 GET 与 303 一律转 GET 并丢掉请求体。
规范说 301/302 该保留方法，但全世界的客户端都转，服务端也按转了写——
跟规范不跟现实，表现是「重定向之后把整个请求体又发了一遍」。

## 20. 「没配 key」和「key 不对」是两个不同的处境

空配置下跑一轮，之前报的是 `auth_failed / API Key 无效`。实际是尚未配置。
两者引导的动作完全相反：一个是「去检查有没有抄错」，一个是「去配一个」。

而且它本不该发出去——`resolveApiKey()` 返回空串时**本地已经知道**没配，
却仍然发一次请求去等 401，再从 provider 的错误文案里猜「是没配还是配错了」。
各家文案不同，猜错的方向恰好是最坏的那个。

现在 `buildAdapter` 在空 key 时直接抛 `no_api_key`，不发请求。唯一豁免是
**baseUrl 指向本机回环**——ollama / LM Studio / vLLM 那里空 key 是合法配置。
判据是主机名不是端口：局域网里另一台机器上的 ollama 不豁免，它可能挂在需要鉴权的
反代后面，静默发空 key 换回来的还是 401，又绕回同一个问题。

配套的两条：

- `qy exec` 在建库之前先体检配置，问题里带**配置文件路径和最小可用示例**——
  provider 回的 401 既不知道配置文件在哪，也不知道该往里写什么，而本地全知道。
- `qy serve` 相反：配置有问题**照样启动**，只打警告。桌面外壳是无条件 spawn 它的，
  这里退出等于应用打不开，而用户唯一能改配置的界面就在应用里。

### 20.1 错误码只有被消费才有意义

同一轮修的还有两处「分类做了但没人用」：

- server 在 loop 之外抛出的错误硬编码成 `internal_error`。因此「没配 key」
  在 CLI 里报 `no_api_key`、在桌面端报 `internal_error`，前端的引导永远不触发。
- 前端的错误卡片只渲染 `message`，`code` 字段逐层传到界面然后被丢掉。
  同一句「请求失败」下面，该去配 key 和该等一分钟是两种完全不同的处境。

错误分类的**唯一目的**是决定用户的下一步动作。一旦压平成 `internal_error`，
或者拿到了却不用，分类这件事就等于没做。

## 21. 打包一次能查出 `cargo check` 永远查不出的问题

「从未跑过 `tauri build`」这条记下来的时候，预期是撞出图标/签名/sidecar 路径问题。
实际撞出的是一个更靠后的问题：**装完之后打开，工作区是安装目录。**

`resolve_workspace()` 的最后一级是 `current_dir()`。从终端里 `cargo run` 时那是仓库根，
完全正常；从开始菜单快捷方式启动时那是安装目录（perMachine 下就是 `C:\Program Files\qywork`）
——既不是用户的代码，又是只读的。这条路径在开发机上一次都走不到。

现在多了一级回落：当前目录**在 exe 所在目录下**或**不可写**时，改用主目录。
可写性只能实地写一个探针文件试——Windows 上光看路径判不出来，
`Program Files` 在提权进程里反而是可写的。

同时查出两条同类的：`package.json` 里 `build:agent` 指向一个不存在的脚本
（`bun run build` 全链路是断的），`version:sync` 同样指向不存在的文件。
**声明了命令不等于命令能跑**，和「协议里有类型 ≠ 有实现」是同一类。

## 22. 一个不可能失败的检查，比没有检查更危险

`sync-version.ts --check` 第一版用 `new Glob('{package.json,packages/*/package.json,...}')`。
Bun 的 Glob **不支持大括号展开**，也不报错——它匹配到 0 个文件，因此
「所有 package.json 的版本号都与 VERSION 一致」这个结论恒为真。

跑出来是绿的，看起来在工作。真正改版本号时会发现 13 个 package.json 一个没动。

这类 bug 的共同形状是：**失败路径被静默地变成了空集**。所以现在扫完之后加了一句
「一个 package.json 都没扫到就直接报错退出」——一个检查必须能证明它确实扫到了目标。

## 23. 只有真实产物才暴露的两条：插件在发布版里根本起不来

接 MCP 期间一并做插件沙箱时，先撞出的不是安全问题，是一条**功能完全失效**：

`PluginHost` 默认拿 `process.execPath` 当插件运行时。开发时那是 `bun`，一切正常。
**而发布产物是 Bun 编译的单文件二进制，`process.execPath` 就是 `qy.exe` 本身**——
拿它跑 `qy <插件入口>` 的结果是「未知命令」加一段用法说明，插件启动即退出。

后果是：插件在开发机上正常，装了包的用户那里一个都起不来，
而这条路径在本机怎么测都测不出来。和第 21 节「装完之后工作区落在 Program Files」
是同一形状——**只在真实产物里才存在的失效**。

现在运行时显式解析：配置指定 > PATH 上的 node > PATH 上的 bun > 宿主自己
（仅当它确实是 node/bun）。都没有就明确报错，不把一个不会执行 JS 的可执行文件
拿去试。有一条测试断言「自动解析出的运行时是 node 或 bun，绝不是宿主二进制」。

### 23.1 反向域名风格的插件 id 会让每一轮 run 被 400 打死

修完上一条，插件终于起来了，然后整轮 run 报：

```
400 Invalid 'tools[0].function.name': string does not match pattern.
Expected a string that matches the pattern '^[a-zA-Z0-9_-]+$'
```

插件 id 是 `demo.lines`——**清单文档自己推荐的反向域名风格**——因此工具名成了
`demo.lines__count`，而 provider 不收带点的名字。一个装了带点 id 插件的用户，
之后**每一句话**都会被 400 打死，错误信息还不说是哪个插件干的。

单测、typecheck、本地跑 agent 全绿，因为内置工具名里没有点。

两处改动：

- `ToolRegistry.register()` **在注册期**校验 `^[a-zA-Z0-9_-]{1,64}$`。装配的人当场
  就能看见是谁的问题，而不是等到发请求时收一条不指名的 400。
- 插件与 MCP 这类名字来自第三方的产出方用 `sanitizeToolName()` 消毒。消毒**会**
  制造碰撞（`a.b` 与 `a_b` 同名），所以产出方必须自己查重并记 failure——
  静默覆盖会无声吞掉一整个插件的工具。

## 24. 插件沙箱：能做到多少就说多少

第 17.1 节承认了「子进程不是沙箱」。现在补上了强制隔离，但**范围必须写准**。

用 node 启动插件并按清单权限开 Node 的权限模型（`--permission`，20~22 上叫
`--experimental-permission`）。只声明 `workspace:read` 的插件，实测：

| 行为 | 结果 |
|---|---|
| 读工作区 | 允许（递归） |
| 读用户主目录 | 拒绝 |
| 写任何位置 | 拒绝 |
| `child_process` | 拒绝 |
| **网络** | **权限模型不管；由出网闸单独挡，见下** |

最后一行**改过**。原来写的是「仍然可用」，并且注明那是这道墙的缺口。
2026-08-10 补上了出网闸（`packages/plugins/src/netguard.ts`），那一面现在有了。
下面这段是新的事实，同样要写准。

### 24.1 出网闸：两个维度分开上报

宿主报的不是一个「有没有沙箱」，是**两个独立的布尔值**，因为它们的成立条件不同：

| 维度 | 靠什么 | 版本要求 | bun 上 |
|---|---|---|---|
| `sandboxed` | Node `--permission` | node 20+ | 没有 |
| `netGuarded` | `module.registerHooks()` 同步钩子 + 拆掉全局出网 API | node 22.15 / 23.5+ | 没有 |

合并成一个值的话，「有沙箱」在不同机器上含义就不一样了——那正是 17.1 纠正过的错。

实测挡住的路径（全部有回归测试盯着，见 `runtime.test.ts` 的「每一条逃逸路径」）：

| 出网路径 | 手段 |
|---|---|
| `import 'node:net'` / `require('net')` | 同步 resolve 钩子（一个钩子同时覆盖 ESM 与 CJS） |
| `node:tls` / `http` / `https` / `http2` / `dgram` / `dns` / `inspector` | 同上 |
| `globalThis.fetch` / `WebSocket` / `EventSource` / `XMLHttpRequest` | 拆掉 |
| `process.binding('tcp_wrap')` | `--permission` 自己就挡了 |
| `process.getBuiltinModule('node:net')` | 绕过模块加载器，单独覆盖 |
| 插件自己 `registerHooks` 短路掉宿主的钩子 | 把 `node:module` 一并列入黑名单 |
| `data:` URL 里再 import | 内层 import 仍走 resolve |

### 24.2 它**不是**什么

三条，必须一起说，否则这一节又会比实现乐观：

1. **`process:exec` 等价于放开网络。** 能起子进程就能跑 `curl`。这不是漏洞是定义：
   授予执行权就是授予「做任何本机能做的事」。所以持有该权限时
   `netGuarded` **如实报 false**，哪怕闸确实注入了。
2. **这是进程内的拆除，不是内核边界。** 准确的说法是
   **「联网从默认可用变成必须刻意绕」**。真正的边界要 OS 级
   （Windows AppContainer / Linux netns 或 seccomp）。
3. **出网闸不管子进程和 worker**，那是权限模型的地盘。
   两套机制对同一件事给出相反答案（发了 `--allow-child-process` 又在模块层挡掉），
   比缺一层防护更难查。

### 24.3 代价

挡 `node:module` 意味着插件用不了 `createRequire`。这是约束 2 的直接后果，
不是可以「优化掉」的——不挡它，插件注册一个 `shortCircuit` 钩子就能把上表全部放行。

另外两条一直成立：

- `--allow-worker` 与 `--allow-addons` **任何权限都换不来**。worker 能另起一套绕开
  权限模型，原生插件直接进内核态调用，而插件没有正当理由需要它们。
- **bun 两样都没有**（既无权限模型也无 `registerHooks`）。宿主在日志里如实写出
  「沙箱 有/无 · 出网闸 有/无」。

### 24.4 一条按预告失效的测试

原来有一条**故意断言「网络仍然可用」**的测试，注释里写着「哪天真堵上了，这条会红，
改它的人必须同时更新这一节」。出网闸落地那天它如约变红了。

保留这段来历不是怀旧：它是这个项目里唯一一条**预告了自己会失效、并且真的按预告失效**
的测试。把已知缺口写成一条会红的断言，比写进文档里有用——文档不会在缺口被补上时提醒你。

## 25. 扩展必须按工作区共享，不能挂在 Session 上

server 是**每条消息新建一个 Session** 的。扩展加载如果绑在 Session 上，
每发一句话就重起一遍全部插件子进程和 MCP server（npx 起的要好几秒），
而旧的那批没有任何人关——`Session.dispose()` 早就写好了，**全项目一个调用点都没有**。

这条之前看不见，是因为插件本来就没人装；MCP 接进来之后每套还多几个 npx 进程，
不可能不炸。

改法不是「记得调 dispose」——那种约束靠不住：

- `loadPlugins` / `loadMcpServers` **只产出 ToolSpec，不再往调用方的 registry 里注册**。
  「加载扩展」和「拿到工具表」解耦。
- 扩展按工作区缓存 + 引用计数。缓存的是 **Promise** 而不是结果：两条消息几乎
  同时进来时，第二条等第一条那次加载，而不是自己再起一遍。
- Session 只借用，`dispose()` 释放引用，归零才真的关子进程。

## 26. 流卡死必须有终态：`stream_idle_timeout` 曾是第七条死链路

跑最后一遍冒烟时撞上：一轮真实 run 挂了 300 秒，只发出 `run.started` 和一次压缩，
之后**一个事件都没有**，也没有终态。

查下去发现 `stream_idle_timeout` 这个 ErrorCode 从协议定稿起就在 union 里，
**全项目没有任何生产者**——第 11 节那张表漏掉的第七条。后果不是「少一种错误分类」，
而是 provider 侧一抖，run 就那么挂着：既不出错也不结束，界面上是一个永远转圈的圈，
账本里躺着一条永远 running 的记录，那个会话从此发不出消息（isBusy 恒为真）。

现在流事件之间有空闲看门狗：

- 计的是**间隔**不是总时长。一轮 agent 跑十分钟是正常的，十分钟里一个字节都没有不是。
- 超时先 **abort 底层请求**再拒绝。不 abort 的话那条连接会一直挂着，
  而它占着的正是刚被判定为不可恢复的那条流。
- abort 用的是**本次尝试专属**的 AbortController（与 run 的 signal 用
  `AbortSignal.any` 链起来）。直接 abort run 的 signal 会把重试和后续步骤一起打死。
- 归 `stream_idle_timeout`，这个码在 `loop.ts` 的重发表里：卡死通常是一过性的，
  一个字节都没收到过就原样自动重发一次。
- 180 秒给得宽，因为首个事件之前要等首 token，长 prompt 上这段本来就慢。
  判错（掐掉一次正常的慢请求）比判漏（无限期挂住）代价大。
- 超时时长可注入，测试用 150ms 验完整条路径——一个要等三分钟的回归测试没人会跑。

**为什么之前六条死链路的排查没抓到它**：那一轮是靠「端到端冒烟断言具体事件出现过」
挖出来的，而这一条是**事件不出现**才成立。断言「X 出现了」抓不到
「什么都没出现」，只有超时才会。第 11 节的结论要补一句：
**冒烟脚本的超时本身就是一条断言**，它响了就说明有路径没有终态。

## 27. 探测器最容易犯的错：把「没测」写成「不支持」

`qy probe` 的价值全在于**它报的每一条都是实测的**。第一版差点毁掉这一点：

OpenAI 兼容适配器不发 `thinking` / `effort` 字段，因此每一个探针都「通过」——
不是因为端点支持，而是因为请求里没有那个字段。报告因此宣称
「支持思考、支持全部 effort 档」，而那份结论会被 `--save` 写回配置，
**覆盖目录里正确的保守值**。

修法不是在探测器里 hardcode「兼容协议就跳过」，而是让适配器**如实声明自己发什么**
（`LlmAdapter.transmits`）。探测器据此把结果分成三种而不是两种：

| 状态 | 含义 | 写回配置吗 |
|---|---|---|
| ✓ | 发了，被接受 | 是 |
| ✗ | 发了，被拒 | 是（是一条结论） |
| – | **没发，无从探测** | **否** |

第三种是关键。它与第二种的区别是「没测」和「测了不行」，把它们合并成一个 false，
就是把没验过的说成验过了。同理，`effortLevels: []`（实测每档都被拒）与
不写这个字段（没测）也必须分开——前者是结论，后者是留白。

还有一处同源的：**「端点不通」与「无从探测」不是一回事**。一个完全可用但不传
thinking 字段的端点，能力一条都探不出来，但它显然是通的。第一版把
`thinking === null` 当成了不通，报出来是「端点不通，先检查 key」——
用户会去查一处没坏的配置。现在 `reachable` 是独立的一位。

## 28. 账目必须比业务数据活得久

`runs` 上的 usage 回答「这一轮花了多少」；「这个月花了多少」它答不了，
因为删会话是正常操作，而 `runs` 是 `ON DELETE CASCADE` 挂在会话上的。

所以用量账本是**独立一张表、没有外键**：`run_id` / `conversation_id` 只是线索，
指向的行没了不影响账目成立。省掉这条约束不是简化，是这张表存在的全部理由。

三条配套约束：

- **一次 run 只写一行**，在收尾时写。中途的 usage 是累计值，每次都记会把同一笔钱
  记很多遍；唯一索引把重复收尾挡住——账目静默翻倍是最难发现的那种错。
- **未回报与真实 0 不合并**。`SUM(cached_tokens)` 全 NULL 时也是 NULL，
  所以要另用 `COUNT(cached_tokens)` 数「有几笔回报过」。混同的表现是界面显示
  「缓存命中 0」——一个具体但错误的结论。
- **摘要调用记 `kind='summary'`**。压缩那次请求不属于任何 run 的 usage，
  在账本出现之前那笔钱**完全看不见**：压缩越频繁，账单和界面上的数字差得越多。

## 29. 前缀审计的边界是「最后一个缓存断点」，没有断点时范围为空

审计范围取错会让这个功能自己废掉，两个方向各有一种废法：

- **取太宽**（把断点之后也算进去）：历史每增长一条就报一次漂移。假警报多了，
  真警报没人看——这比不审计更糟，因为它制造了「已经在看着了」的错觉。
- **取太窄**：漏掉真正冲缓存的改动，而缓存失效本来就是静默的，漏了就永远不知道。

所以判据是「最后一个 `cacheBreakpoint`（含）之前」。**一个断点都没有时范围是空**，
不是「全部」——没有声明冻结区就等于没有要审计的范围，判成全部会落进第一种废法。

哈希时段之间要插分隔符：`["ab",""]` 与 `["a","b"]` 直接相连是同一串字节，
但它们是不同的前缀（分段方式影响缓存断点位置）。

静态与运行时两层缺一不可：静态只认得出已知的坏模式（日期、路径、uuid），
真正的漂移往往来自「在前缀里拼了个变量」这种没见过的写法；
而运行时审计要等真的发了请求才知道，拦不住改动落地。

## 30. 导出的两种格式取舍相反，所以不能合并

- **markdown 给人读**：成功的工具折叠成一行，**失败的展开带正文**。
  一视同仁地折叠或展开，都会让这份文档在最有用的地方最没用——
  成功的调用读者基本不看，失败的才是最需要细节的。
- **json 给脚本读**：**完全不裁剪**。裁剪等于把「导出的内容不全」藏起来，
  而脚本没法像人一样看出「这里少了点什么」。

合成一种格式必然要在「可读」和「完整」之间选一头，两边都不好用。

压缩过的会话在文档最上面标出来：读者看到的原文是完整的，模型看到的是摘要。
不说的话，「模型为什么忘了前面」会变成一个查不出原因的问题。

## 31. 交互模式做成行式 REPL，不做全屏

全屏方案（备用缓冲区、自绘光标、鼠标）在 Windows 的 conhost 上是雷区：
resize 事件、宽字符光标定位、Ctrl-C 的传递各有各的坑。它换来的收益——固定输入框、
独立滚动区——对一个「说一句、看它执行」的循环并不是必需的。
行式 REPL 把渲染交给终端本身，代价是界面朴素，收益是它在哪都能跑。

三条不能省的行为：

- **非 TTY 下打印用法而不是进循环**。`qy | cat` 或 CI 里等输入，表现是「命令挂住了」，
  而那是最难排查的一种失败。
- **跑的时候 Ctrl-C 中断这一轮、不退出**；空闲时才退出。一个改到一半的任务
  被整个进程带走，比中断本身糟得多。
- **未知斜杠命令明确拒绝**，不落到「当成提问发出去」。打错一个斜杠收到一段模型回答
  是最让人困惑的反馈，而且要为它付钱。

与 `qy exec` 的差别不是「加个循环」：交互模式**跨轮复用同一个 conversationId**，
模型看得到上一轮、缓存也能命中；exec 每次都是新会话，那正是「一次性执行」
应该有的语义。两者合并会让其中一个的语义变错。

## 32. Responses 协议的差别在**形状**，不在路径

接 `openai_responses` 时最容易犯的错是把它当成「chat/completions 换个 URL」。
三处形状差异，每一处按 chat 协议写都会得到一个**结构合法但语义错误**的请求，而且不报错：

| | chat/completions | responses |
|---|---|---|
| 消息容器 | `messages` | `input`，是**条目序列** |
| 工具调用 | 挂在 assistant message 的 `tool_calls` 字段上 | **顶层条目** `function_call` |
| 工具结果 | `role:'tool'` 的消息 | **顶层条目** `function_call_output` |
| 工具定义 | `{type:'function', function:{name,...}}` | **扁平** `{type:'function', name,...}` |
| 文本块 | 统一 `content` 字符串 | 输入侧 `input_text`／输出侧 `output_text` |
| 流式 | delta 拼接 | 有类型的 SSE 事件 |

「按 chat 协议写工具调用」的后果最隐蔽：请求发得出去、模型也回得来，但它**看不到自己调过什么**，
因此会把同一个工具反复调下去。没有任何错误信息指向这一点。

还有两条不在形状上：

- **`max_output_tokens` 同时封顶思考与正文**。按「不思考」的口径调小它，
  回答会从中间被截断——与 Anthropic 那条坑同源。
- **`input_tokens` 含缓存命中**，与 Anthropic 的排他口径相反。不减的话缓存命中越多、
  账单偏差越大（同一个问题在 OpenAI 兼容适配器上实测出现过，见第 5.3 节）。

实现上直接打 HTTP 不用 SDK：Responses 的 SDK 类型随版本变动频繁，而这里要处理的
字段集（推理条目、各家中转站的扩展字段）本来就得按 `Record` 断言。
引一层类型再全部 `as never`，等于既付了依赖又没拿到类型收益。

**没有对着真实端点跑过**——手上没有 OpenAI key。单测覆盖了上面每一条形状与口径，
但「真发一次请求」这件事没做，如实记在这里而不是让它看起来已经验证过。

## 33. shell 沙箱：写边界、读不管，以及为什么自己拼 bwrap

`run_command` 之上原来有三层，全是**文本判断**：硬边界看变量名与值、
静态规则是一张表、分类器是概率。三层都挡不住一条没想到的写法，
而「没想到」按定义列不完。所以真正的边界只能在内核层。

### 33.1 分档报，而且报的是**实测**不是**查到**

| 平台 | 后端 | 有没有 |
|---|---|---|
| Linux / WSL2 | bubblewrap | ✅ 装了 `bwrap` 且实测跑得起来 |
| macOS | seatbelt（`sandbox-exec`） | ✅ 系统自带，实测跑得起来 |
| 原生 Windows | — | ❌ 未接 |
| WSL1 | — | ❌ 无独立内核，namespace 不可用 |

**不合并成一个「有沙箱」的布尔值**——这与第 24.1 节插件那两个维度是同一条理由，
而那条理由本身来自 17.1 纠正过的错。`qy config` 每次都报，有没有都报：
一个没有内核边界的平台上，把「shell 被拦得住」当成默认前提是这套模型最危险的误解。

**「二进制在 PATH 里」和「它能建出命名空间」是两件事。** 第一版只查 `which`，
那是错的：Ubuntu 24.04+ 默认开着 `kernel.apparmor_restrict_unprivileged_userns`，
`unshare(CLONE_NEWUSER)` 会成功但新命名空间里没有 capability——bwrap 装着、
一条命令都跑不起来，而检测结果会报「有边界」。**这个方向的错是最坏的一种。**

所以 `detectSandbox()` 真的执行一次空命令，失败就降级并把 stderr 第一行连同
修法一起报出来。代价是一次进程启动，整个进程生命周期只付一次。

这条同时是 macOS 那一档的保证方式：**本仓库没有 Mac**，seatbelt 的 profile
只有纯函数测试。真正确认它可用的是用户机器上那次自检——
CI 保证形状，运行期保证可用性。这比「写了但没人跑过」诚实得多，
也比「因为验不了所以不做」有用得多。

### 33.2 是写边界，不是读边界

`--ro-bind / /` 打底，可写根目录逐个 `--bind` 开口子。所以：

- 工作区外的**普通文件仍然读得到**；
- 凭证目录（`~/.ssh`、`~/.aws`、`~/.qywork` 等）是**单独点名 `--tmpfs` 盖空**的。

做成读边界要只挂少数几个目录，而那样 `/usr`、`/lib`、`/etc` 全没了，
基本什么都跑不起来。**所以那份凭证目录清单是列举，和静态规则一样会漏**——
区别只在代价：这里漏一条是少屏蔽一个目录，静态规则漏一条是放行一条命令。

同理，**家目录不整个盖掉**：盖了 `~/.gitconfig`、`~/.npmrc`、nvm/rustup 全消失，
`git commit` 没有作者、`node` 可能找不到。那种沙箱用户开一次就会关掉，
而关掉之后一层都不剩。

### 33.3 网络只有两档，没有中间态

默认不加 `--unshare-net`：断网的 agent 装不了依赖、拉不了代码，而报错跟网络
毫不相干（包管理器只会说拉取失败）——查到原因之前用户会先把整个沙箱关掉，
因此文件边界也一起没了。

`sandboxNetwork: "deny"` 可以整个断掉（bwrap 加 `--unshare-net`，
seatbelt 加 `(deny network-outbound)`）。

**中间态（按域名白名单）刻意不做。** 它需要在沙箱里起代理、沙箱外做转发、
还要让 TLS 校验认一张自签 CA。那套组件会在别人的机器上以各种方式坏掉，
而坏掉的表现是「网络时好时坏」——比没有这个功能糟得多。

保持默认时，shell 命令的出网**没有任何边界**（`web_fetch` 有 SSRF 闸，shell 没有）。
如实写在 `docs/permissions.md` 的「已知边界」里。

### 33.6 两个后端形状不同，承诺必须相同

bwrap 是挂载表，seatbelt 是规则集，表达方式完全不同。但
`docs/permissions.md` 只有一张边界表——两边的承诺一旦分叉，那份文档就得
按平台拆开写，而拆开的文档没人维护得住。

所以有一组测试**专门比对两个后端**：可写根、`.qy/` 只读、凭证目录、网络，
逐条断言两边都做了同一件事。它不验任何一个后端是否正确，
只验**它们没有各说各话**。

一处必然的差别写在文档里而不是藏起来：bwrap 把凭证目录**盖成空的**
（看得见但是空），seatbelt 没有挂载这回事，只能**拒绝读**（存在但打不开）。
防的行为一样，报错文案会不同。

### 33.4 顺序是这一层唯一的承重结构

bwrap 按参数出现顺序叠加，后到的覆盖先到的。两条已经撞过：

- **`--tmpfs /tmp` 必须排在可写 `--bind` 之前。** 反了的话，一个位于 `/tmp` 下的
  工作区会被随后的 tmpfs 整个盖掉，而且**不报错**——命令照常执行，
  只是工作区在里面是空的。
- **`.qy/` 的 `--ro-bind` 必须排在可写 `--bind` 之后**，否则它会被重新变成可写。

第一条是真跑 WSL2 撞出来的：参数生成得完全正确，错的是两条正确参数之间的顺序。
纯函数断言看不见这类错误——它们只在真实内核里才有意义。

### 33.5 为什么不用 `@anthropic-ai/sandbox-runtime`

评估过（0.0.71，Apache-2.0），功能远比这里全，四条对不上：

1. **网络隔离在 Linux 上关不掉。** `needsNetworkRestriction` 由
   `network.allowedDomains !== undefined` 决定，而那是 schema 必填字段，
   因此 bwrap 一定带 `--unshare-net`。
2. **依赖装不上。** Linux 侧要 bwrap + socat + ripgrep，本机 WSL2 只有 bwrap，
   且没有外网。
3. **单文件产物带不了它的 vendor 二进制**（`apply-seccomp`、`srt-win.exe`）——
   与第 24 节 netguard 是同一个坑。
4. **验不了的能力不能当边界发出去。**

它另有一个 **Windows 后端**（`srt-win.exe` + WFP 出网栅栏 + 专用 `srt-sandbox`
本地账户），需要一次提权安装。这件事推翻了「Windows 上没有内核边界的实现」
这个说法——那句话之前在本仓库出现过多次，是错的。


---

## 34. 握手被拒一律终态：一个永远不会好的「稍后重试」

客户端原来写的是 `this.closed = msg.reason === 'bad_token'`——只有令牌无效
才停止重连。而服务端**真会发的是两种**：`bad_token` 和 `protocol_mismatch`。

因此协议版本对不上时：socket 被关 → `close` 事件 → 退避重连 → 再次被同样地拒掉，
**每 ≤15 秒一轮，永远**。界面上先闪一下「服务端协议版本 N，客户端 M」，
然后变成「N 秒后重试」。

**这是最坏的一种错误提示：它把一个需要用户动手的失败，显示成了一个会自愈的失败。**
版本不匹配永远不会因为再连一次而变好，要改的是某一端的代码。

处置是一律终态，**并且不预留 `reason !== 'too_many_clients'` 这类分支**——
那个取值在服务端没有任何生产者，留一个没人走的分支只会让下一个读代码的人
把它当成生效过的逻辑。真出现「等等就好」的原因时再加。

一并记着：这条**没有回归测试**。`client.ts` 要真的 WebSocket 才能跑，
本仓没有浏览器侧测试装置，为一个布尔常量搭一套不值当——所以它属于
「改了、类型过了、行为没验」那一类。

---

## 35. 两个节奏都不该等于网络的节奏

界面上有两处的快慢，直觉上都该跟着数据走，实际上都不能。

### 35.1 正文的呈现节奏

`text.delta` 到达的节奏是网络的节奏。provider 卡一下、攒一批、一次性吐出来——
直接写进 transcript 的话，画面就跟着网络一起顿，一次突发砸出一屏字。
**用户看到的是「卡了」，实际是「来得太快」。**

处置是在收到和显示之间加一个缓冲区，按 50ms 一档往外放
（`apps/web/src/lib/stream-pace.ts`）。
三个常数各挡一种坏情况，其中**硬顶那条最容易被漏掉**：软目标是「积压 ÷ 24 档放完」，
积压一万字时那也是一次 417 字——照样一帧糊出去。

关键在于**缓冲区不能变成第二本账**。靠两条纪律：

1. 除 `text.delta` 外的**任何**事件先冲一次。读数条、错误卡、工具卡读的是
   同一份 transcript，不能让它们看到一段放了一半的正文。这条同时保证缓冲区里
   永远只有当前尾部那一段，不必按 step 记账。
2. 换会话 / 整段重拉是**丢弃**，不是冲出去。那段字属于重拉之前的那份 transcript。

**没有实测数据支撑常数取值**——只测了「不丢字」（这套机制唯一不可接受的失败）。
定时器编排本身也没有自动化测试。如实记着。

### 35.2 与 60ms 重解析节流不是一回事

同一个文件里还有一个 60ms：`Prose` 的 markdown 重解析节流。两者常被当成一件事，
但限的对象不同——**那个省 CPU（16K 字回复尾部每 delta 重解析要 39.6ms，一帧才 16.7ms），
这个给眼睛看**。叠加不冲突，删掉任何一个都会露出各自的症状。

---

## 36. `no_progress`：`max_steps` 之外，还有一种停不下来

模型调一个工具、没解决问题、**用一模一样的参数再调一遍**、拿到一模一样的结果。
这种情况会一直跑到 `maxSteps`：几十轮 provider 往返，钱花了、
最后报一个「已达步数上限」。

**那个终止原因本身是错的**，而且错得很有害：用户据此去调大步数上限，只会烧更多。
它不是步数不够，是原地打转，多给一百步也一样。

判据要三样都不变才算空转，缺一条就会误判（`packages/agent/src/progress.ts`）：

- **只看动作不看结果会误判轮询。** 等构建、等文件出现都是「同样的命令、
  不同的输出」，那是有进展的。
- **必须看副作用。** 反复写同一个文件、每次内容不同，工具可能都只回「已写入」，
  但它确凿在执行。取的是执行器给出的 `fileChanges` 事实，不是猜的。
- **失败本身不算证据。** 一次报错不能证明副作用没发生（写了一半再抛也是错），
  所以含糊的失败留着可重试。

### 两处刻意放宽

1. **判在批次跑完之后，不在下发之前。** 工具是整批下发的，提前中断会在
   transcript 里留下一条「有 tool_calls 却没有 tool 结果」的 assistant 消息——
   **下一轮请求发给 provider 直接 400**。晚一轮的代价是多跑一个批次。
2. **要求三个周期，不是两个。** 连着两次一模一样在真实使用里可能是正常的
   （模型重新定位时再看一眼同一个目录）。**三次就没有良性解释了。**

第二条是这个改动里唯一有分寸感的地方，值得单独说：**这条规则的全部风险都在
误判那一侧**。漏判的代价是多烧几轮；误判的代价是把一个正常的长流程砍掉，
而用户看到的是「它自己停了、活没干完」，界面上没有任何线索指向这条规则。
所以宁可晚一个周期。

实测（`loop.test.ts`，脚本给足十轮）：空转的那个在**第三轮**停下，
轮询的那个跑满四轮正常收尾。
